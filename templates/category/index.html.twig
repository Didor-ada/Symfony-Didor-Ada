{% extends 'base.html.twig' %}

{% block title %}All categories{% endblock %}

{% block body %}

    <h1>tiens tes catégories : </h1>
    {% for categorie in categories %}
        <div>
            <h2>Catégorie : <a href="{{ path('category_show', {categoryName:categorie.name}) }}">{{ categorie.name }}</a></h2>


        </div>
    {% endfor %}

    <a href="{{ path('app_index') }}">
        Retour à l'accueil
    </a>
{% endblock %}
